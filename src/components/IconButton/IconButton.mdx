import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks'
import * as IconButtonStories from './IconButton.stories'

<Meta of={IconButtonStories} />

# IconButton

{/* BEGIN: when-to-use */}

## When to use

- A floating icon-only affordance that has to stay legible over arbitrary imagery — carousel
  arrows, overlay dismissals.
- A control that should look identical regardless of the surrounding theme.

## When not to use

- For an icon-only control that should follow the app's theme (for example, inline on a themed
  surface), use [Button](?path=/docs/actions-button--docs) with `round`, `clear`, and `icon`
  instead, because `IconButton` is deliberately theme-independent and won't flip in dark mode.
- For an action with visible text, use [Button](?path=/docs/actions-button--docs) instead,
  because `IconButton` has no children slot — it renders only the icon.

{/* END: when-to-use */}

{/* BEGIN: anatomy */}

## Anatomy

The circular control and the glyph it centres. There is no label part — the button is icon-only, so its accessible name comes from `aria-label`.

<Canvas of={IconButtonStories.Anatomy} />

| Part | `data-part` | Renders    | Purpose                                                             |
| :--- | :---------- | :--------- | :------------------------------------------------------------------ |
| Root | `root`      | `<button>` | The circle: fixed size, radius, and focus ring                      |
| Icon | `icon`      | `<svg>`    | The glyph, sized by `small` and coloured independently of the theme |

{/* END: anatomy */}

{/* BEGIN: when-to-use */}

## Related

- The themed, labelled equivalent for actions with visible text — see
  [Button](?path=/docs/actions-button--docs).
- The icon set `IconButton` draws its `name` prop from — see
  [Icon](?path=/docs/media-content-icon--docs).

{/* END: when-to-use */}
