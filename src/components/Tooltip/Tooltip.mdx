import { Anatomy } from '@component-anatomy/storybook/blocks'
import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks'
import * as TooltipStories from './Tooltip.stories'

<Meta of={TooltipStories} />

# Tooltip

{/* BEGIN: anatomy */}

## Anatomy

<Canvas of={TooltipStories.Anatomy} />

<Anatomy of={TooltipStories.Anatomy} />

<span className="sb-sr-only">

| Part    | `data-part` | Renders           | Purpose                                     |
| :------ | :---------- | :---------------- | :------------------------------------------ |
| Trigger | `trigger`   | the child element | Rendered as the trigger itself, not wrapped |
| Popup   | `popup`     | `<div>`           | The tip; portalled outside the canvas       |

</span>

{/* END: anatomy */}

{/* BEGIN: props */}

## API reference

<ArgTypes of={TooltipStories} />

### `label`

The tip's content — it repeats the trigger's name, never replaces it.

<Canvas of={TooltipStories.Label} />

### `children`

Becomes the trigger itself rather than being wrapped in one.

<Canvas of={TooltipStories.Children} />

### `sideOffset`

The gap between the trigger and the tip.

<Canvas of={TooltipStories.SideOffset} />

### `className`

Merged onto the popup alongside the component's own `droppy-Tooltip` class.

<Canvas of={TooltipStories.ClassName} />
{/* END: props */}
