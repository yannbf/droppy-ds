import { Anatomy } from '@component-anatomy/storybook/blocks'
import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks'
import * as NumberFieldStories from './NumberField.stories'

<Meta of={NumberFieldStories} />

# NumberField

{/* BEGIN: when-to-use */}

## When to use

- A quantity or amount where the caller can't predict every value in advance, or where keyboard
  and direct-entry access matter — a cart quantity with a wide range, a price, a form field
  counting units of something.

## When not to use

- A small, bounded range where tap-tap increments are the whole interaction and typing isn't
  expected — reach for [QuantityStepper](?path=/docs/forms-input-quantitystepper--docs) instead;
  it is the simpler, button-only sibling of this component.

{/* END: when-to-use */}

{/* BEGIN: anatomy */}

## Anatomy

<Canvas of={NumberFieldStories.Anatomy} />

<Anatomy of={NumberFieldStories.Anatomy} />

<span className="sb-sr-only">

| Part       | `data-part` | Renders    | Purpose                                             |
| :--------- | :---------- | :--------- | :-------------------------------------------------- |
| Field      | `field`     | `<div>`    | Base UI's `Field.Root`, associating label and input |
| Root       | `root`      | `<div>`    | Owns the value, the bounds, and the formatting      |
| Scrub area | `scrub`     | `<span>`   | Wraps the label; dragging it steps the value        |
| Label      | `label`     | `<label>`  | The visible label and accessible name               |
| Group      | `group`     | `<div>`    | Holds the buttons and the input                     |
| Decrement  | `decrement` | `<button>` | Disabled at `min`                                   |
| Input      | `input`     | `<input>`  | The typeable control                                |
| Increment  | `increment` | `<button>` | Disabled at `max`                                   |

</span>

{/* END: anatomy */}

{/* BEGIN: when-to-use */}

## Related

- [QuantityStepper](?path=/docs/forms-input-quantitystepper--docs) — a simpler, button-only
  stepper for small bounded counts where typing isn't needed.
- [Input](?path=/docs/forms-input-input--docs) — the plain text field this shares its labelling
  approach with.

{/* END: when-to-use */}
