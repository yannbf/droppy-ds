import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks'
import * as SeparatorStories from './Separator.stories'

<Meta of={SeparatorStories} />

# Separator

{/* BEGIN: general */}

A visual and semantic divider between two blocks of content.

```tsx
import { Separator } from '@droppy/design-system'
```

<Canvas of={SeparatorStories.Default} />

## Features

- **`orientation` (default `"horizontal"`) sets both the visual rule direction and the
  `aria-orientation`/`data-orientation` attributes** — pass `"vertical"` for a divider between
  inline content.
- Renders `role="separator"`, so assistive technology recognizes it as a division between
  content rather than decoration.
- Merges any incoming `className` onto the root, alongside the theme layer's own class.

{/* END: general */}

{/* BEGIN: behavior */}

## How it works

`Separator` renders `role="separator"` unconditionally, so it is a semantic divider rather than a
decorative line. For a purely ornamental rule with no grouping meaning, a styled border is the
lighter choice — there is no presentational escape hatch here.
{/* END: behavior */}

{/* BEGIN: anatomy */}

## Anatomy

`Separator` is a single-part component: one `<div>` with `role="separator"` and no children. The caller sizes it — width or height in CSS — to read visually as a line.

<Canvas of={SeparatorStories.Anatomy} />

| Part | `data-part` | Renders | Purpose                                                                              |
| :--- | :---------- | :------ | :----------------------------------------------------------------------------------- |
| Root | `root`      | `<div>` | The divider, carrying `role="separator"`, `aria-orientation`, and `data-orientation` |

{/* END: anatomy */}
