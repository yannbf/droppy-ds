import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks'
import * as SeparatorStories from './Separator.stories'

<Meta of={SeparatorStories} />

# Separator

{/* BEGIN: anatomy */}

## Anatomy

`Separator` is a single-part component: one `<div>` with `role="separator"` and no children. The caller sizes it — width or height in CSS — to read visually as a line.

<Canvas of={SeparatorStories.Anatomy} />

| Part | `data-part` | Renders | Purpose                                                                              |
| :--- | :---------- | :------ | :----------------------------------------------------------------------------------- |
| Root | `root`      | `<div>` | The divider, carrying `role="separator"`, `aria-orientation`, and `data-orientation` |

{/* END: anatomy */}

{/* BEGIN: do-dont */}

## Do's and don'ts

### Do

- Reach for `Separator` where the break carries meaning, e.g. between order-summary lines or
  clusters of controls
- Match `orientation` to the axis it divides on: it sets `aria-orientation` as well as the visual
  rule
- Supply the spacing around a separator yourself, as the component declares no margin of its own

### Don't

- Don't use `Separator` for pure decoration: every instance is announced as a separator, so a
  purely visual rule is better drawn with a border

{/* END: do-dont */}
