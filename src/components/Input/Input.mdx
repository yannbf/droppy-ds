import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks'
import * as InputStories from './Input.stories'

<Meta of={InputStories} />

# Input

{/* BEGIN: anatomy */}

## Anatomy

A field root wrapping a label, the control, and an always-mounted error slot. The slot is the unusual part: it stays in the DOM holding a non-breaking space when there is no message, which is what reserves the height.

<Canvas of={InputStories.Anatomy} />

| Part    | `data-part` | Renders   | Purpose                                                          |
| :------ | :---------- | :-------- | :--------------------------------------------------------------- |
| Root    | `root`      | `<div>`   | Base UI's `Field.Root`, wiring label, control and error together |
| Label   | `label`     | `<label>` | Absent when `label` is empty                                     |
| Control | `control`   | `<input>` | The control itself; `className` lands here                       |
| Error   | `error`     | `<div>`   | Always mounted, so showing a message never moves the layout      |

{/* END: anatomy */}

{/* BEGIN: a11y */}

## Keyboard & screen reader

| Key                        | Action                                                                  |
| -------------------------- | ----------------------------------------------------------------------- |
| Tab / Shift+Tab            | Moves focus to or from the field                                        |
| Standard text-editing keys | Native browser text editing — the rendered element is a plain `<input>` |

- **ARIA:** Field auto-wires `for`/`id` (label → control) and `aria-describedby` (error slot → control); the input carries `aria-invalid` while its Field considers it invalid.
- **Screen reader:** announces the label as the field's accessible name, then reads the error slot as part of the field's description. Because the slot is always rendered — even with no error — its id stays in `aria-describedby` at all times; with no error present, a screen reader simply reads a blank description.
- **You must provide:** a `label`. Without one, no `Field.Label` renders at all and the input has no accessible name — the same obligation every native form control has.

{/* END: a11y */}
