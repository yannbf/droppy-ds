import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks'
import * as ProgressBarStories from './ProgressBar.stories'

<Meta of={ProgressBarStories} />

# ProgressBar

{/* BEGIN: anatomy */}

## Anatomy

Two parts: the track carries the `progressbar` role and the whole ARIA range, and the fill is a plain width-driven sibling underneath it with no semantics of its own. There is no label part — the copy around the bar is the caller's own markup.

<Canvas of={ProgressBarStories.Anatomy} />

| Part | `data-part` | Renders | Purpose                                                              |
| :--- | :---------- | :------ | :------------------------------------------------------------------- |
| Root | `root`      | `<div>` | `role="progressbar"` with `aria-valuenow`/`-min`/`-max` and the name |
| Fill | `fill`      | `<div>` | The width-driven bar; purely visual                                  |

{/* END: anatomy */}

{/* BEGIN: props */}

## API reference

<ArgTypes of={ProgressBarStories} />

### `value`

Positions the fill, from empty through to full. Clamped to `0`–`max` before it reaches the DOM.

<Canvas of={ProgressBarStories.Value} />

### `max`

The upper bound — step counts, not just percentages.

<Canvas of={ProgressBarStories.Max} />

### `label`

The accessible name. Nothing visible is rendered from it; the title and copy around the bar live in
the caller's own markup.

<Canvas of={ProgressBarStories.Label} />

### `className`

Merged onto the root alongside the component's own `droppy-ProgressBar` class.

<Canvas of={ProgressBarStories.ClassName} />
{/* END: props */}
