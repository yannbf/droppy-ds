import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks'
import * as ReviewStories from './Review.stories'

<Meta of={ReviewStories} />

# Review

{/* BEGIN: general */}

A star-rating and text line — "★ 4.5 Very good" or "No reviews yet" — for a restaurant tile, an
item card, or an order summary.

```tsx
import { Review } from '@droppy/design-system'
```

<Canvas of={ReviewStories.Default} />

## Features

- `rating` is a number out of 5. It maps to one of four labels — "Very poor" below 2, "Adequate"
  from 2 to 4, "Very good" from 4 to 5, "Excellent" at 5 — prefixed with the star glyph and the
  rating to one decimal place.
- No `rating` (or a falsy one) renders "No reviews yet" instead of a score.
- Uses the low-contrast `--ds-color-text-review` token by default — a deliberately quiet
  treatment so the rating reads as secondary to the content around it. Pass `color` to override it.
- Merges any incoming `className` with its own `droppy-Review` class.

{/* END: general */}

{/* BEGIN: when-to-use */}

## When to use

- Alongside a restaurant or item's name, to show its aggregate rating at a glance.

## When not to use

- A star-input control for a visitor to leave their own rating — `Review` is a read-only display,
  not a form control.

{/* END: when-to-use */}

{/* BEGIN: behavior */}

## How it works

### Rating bands

The bands are fixed: under 2 is "Very poor", 2–4 "Adequate", 4–5 "Very good", and exactly 5
"Excellent". The number always shows one decimal, so `4` and `4.0` read identically.

<Canvas of={ReviewStories.RatingBands} />

### No rating

A missing or falsy `rating` renders a plain fallback line rather than a zero-star score, so a
newly-listed restaurant doesn't look badly reviewed.

<Canvas of={ReviewStories.NoReviews} />
{/* END: behavior */}

{/* BEGIN: anatomy */}

## Anatomy

A wrapper and the text line inside it. The star, the number, and the band label are one string rather than separate parts — there is no per-star markup to target.

<Canvas of={ReviewStories.Anatomy} />

| Part | `data-part` | Renders  | Purpose                                        |
| :--- | :---------- | :------- | :--------------------------------------------- |
| Root | `root`      | `<div>`  | The wrapper the caller positions               |
| Text | `text`      | `<span>` | The score line, in the quiet review text token |

{/* END: anatomy */}

{/* BEGIN: examples */}

## Examples

### Mealdrop restaurant tiles

<Canvas of={ReviewStories.MealdropRestaurantScores} />

{/* END: examples */}

{/* BEGIN: do-dont */}

## Do's and don'ts

### Do

- Hand `rating` over unrounded and let `Review` format it: it renders one decimal place and picks
  the wording ("Very good", "Excellent") from the value
- Leave `rating` unset where nothing has been rated yet; the component says "No reviews yet"
  rather than showing a misleading zero

### Don't

- Don't set `color` to make a rating stand out: the quiet, low-contrast treatment is deliberate,
  and the accent-as-ink exception it already uses isn't meant to be extended
- Avoid placing a `Review` where the thing being rated isn't obvious from the line above it

{/* END: do-dont */}

{/* BEGIN: a11y */}

## Keyboard & screen reader

`Review` renders static text — there is no keyboard interaction to document.

- **ARIA:** none — the rating and label are read as plain text content.
- **Screen reader:** announced as the rendered string (e.g. "★ 4.5 Very good" or "No reviews yet").
- **You must provide:** nothing further, unless the surrounding context needs an accessible name
  for the region the review sits in.

{/* END: a11y */}

{/* BEGIN: brand */}

## Brand

Review-star text is one of the two deliberate exceptions to the
[brand guidelines](?path=/docs/brand-guidelines--docs)' "Accents are surfaces, not ink". The
exception is implemented asymmetrically per theme: light theme darkens the accent to `blue-700`
so it works as ink, dark theme uses the brand's pale blue verbatim. Don't extend the exception —
other text keeps ink colors, with accents as backgrounds.

{/* END: brand */}

{/* BEGIN: styling */}

## Styling

| Token                    | Used for                              |
| ------------------------ | ------------------------------------- |
| `--ds-color-text-review` | Default text color (low-contrast)     |
| `--ds-type-size-sm`      | Text size, via `Body`'s `S` size step |

`className` merges with the component's own `droppy-Review` class. `color` overrides the token
inline.

{/* END: styling */}

{/* BEGIN: props */}

## API reference

<ArgTypes of={ReviewStories} />

### `rating`

Drives both the number and the word beside it.

<Canvas of={ReviewStories.Rating} />

### `color`

Overrides the deliberately quiet default (`--ds-color-text-review`) — for a caller like Mealdrop's
restaurant tiles that wants its own treatment.

<Canvas of={ReviewStories.Color} />

### `className`

Merged onto the root alongside the component's own `droppy-Review` class.

<Canvas of={ReviewStories.ClassName} />
{/* END: props */}

{/* BEGIN: history */}

## Decision log

- **2021-01**: Review was added to Mealdrop as a single text line; a real `<Icon name="star"/>` was sketched in the first draft but never shipped, and the literal ★ glyph baked into the label string remains to this day ([review component](https://github.com/yannbf/mealdrop/commit/21d209f4bfc8f4bf96411e52ddad39915676c955), [icon sketch removed](https://github.com/yannbf/mealdrop/commit/8a0a9fedd786deb75865776e61b4b6beffe38912)).
- **2021-12**: the rating-to-label banding settled on exclusive ranges — below 2 "Very poor", 2–4 "Adequate", 4–5 "Very good", 5 "Excellent" — after two buggy iterations whose overlapping comparisons made 2 and 4 ambiguous; Droppy ports this final logic verbatim ([banding fix](https://github.com/yannbf/mealdrop/commit/1f5e590b8c73f2bd996b36e69e1a893d9dc13e35)).
- **2026-08**: ported to Droppy on token-driven CSS with a new `color` escape hatch for opting out of the deliberately quiet default ink ([initial commit](https://github.com/yannbf/droppy-ds/commit/00b19b5a1be01ae266251a71bd074e28ad8cd267)).
- **2026-08**: the review ink implements the brand's pale-blue exception asymmetrically per theme: light theme darkens the accent (`blue-700`) so it works as ink, dark theme uses the brand's pale blue verbatim — per the [brand guidelines](?path=/docs/brand-guidelines--docs), review-star text is one of the two deliberate exceptions to "accents are surfaces, not ink".
- **Built on `Body`, sized `S` and rendered as a `span`.** Droppy supplies the props, the theme classes, and the copy around it.

{/* END: history */}

{/* BEGIN: known-issues */}

## Known issues & open questions

- [#276](https://github.com/yannbf/droppy-ds/issues/276): `rating={0}` and `NaN` both render "No reviews yet" — a genuine zero score, or a division-by-zero upstream, is indistinguishable from the empty state. **Workaround:** validate upstream and pass `undefined` only when there are truly no reviews.
- [#277](https://github.com/yannbf/droppy-ds/issues/277): out-of-range ratings render unclamped — `rating={9}` gives "★ 9.0 Excellent" and negative values fall through to "★ -2.0 Very poor". **Workaround:** clamp at the call site before passing (`Math.min(Math.max(rating, 0), 5)`).
- [#278](https://github.com/yannbf/droppy-ds/issues/278): the star is a bare ★ text glyph with no ARIA — screen readers announce the line as plain text with no "out of 5" scale context, and rendering depends on the active font's glyph coverage. **Workaround:** wrap Review in an element with an explicit `aria-label` (e.g. "Rated 4.5 out of 5").
- [#125](https://github.com/yannbf/droppy-ds/issues/125): the prop type is a closed set, so `id`/`data-testid`/`aria-*`/`style`/event handlers never reach the rendered DOM.

{/* END: known-issues */}

{/* BEGIN: when-to-use */}

## Related

- [Body](?path=/docs/typography-body--docs) — the typography primitive `Review` builds on.

{/* END: when-to-use */}
