import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks'
import * as PageTemplateStories from './PageTemplate.stories'

<Meta of={PageTemplateStories} />

# PageTemplate

{/* BEGIN: anatomy */}

## Anatomy

A column of header, content, and footer. Only the content area is a part the template owns — header and footer are whatever nodes the caller passes, rendered as-is outside the landmark.

<Canvas of={PageTemplateStories.Anatomy} />

| Part    | `data-part` | Renders  | Purpose                                                         |
| :------ | :---------- | :------- | :-------------------------------------------------------------- |
| Root    | `root`      | `<div>`  | The column: header, content, footer in order                    |
| Content | `content`   | `<main>` | The landmark, carrying the min-height that pins the footer down |

{/* END: anatomy */}

{/* BEGIN: do-dont */}

## Do's and don'ts

### Do

- Render one `PageTemplate` per page, at the app-layout level: it carries the page's `<main>`
  landmark
- Pass the site-wide header and footer through `header` and `footer`, so they stay outside `<main>`
  and out of the main-content landmark
- Put the app's own skip link ahead of the template; `PageTemplate` adds none of its own

### Don't

- Avoid header or footer content much taller than the usual chrome: the content area reserves a
  fixed allowance for it, and a taller slot pushes the footer below the fold on short pages

{/* END: do-dont */}
