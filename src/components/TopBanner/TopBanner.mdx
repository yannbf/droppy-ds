import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks'
import * as TopBannerStories from './TopBanner.stories'

<Meta of={TopBannerStories} />

# TopBanner

{/* BEGIN: anatomy */}

## Anatomy

A band with an optional heading. Both are optional: with neither `title` nor `photoUrl` the band renders empty.

<Canvas of={TopBannerStories.Anatomy} />

| Part  | `data-part` | Renders | Purpose                                                        |
| :---- | :---------- | :------ | :------------------------------------------------------------- |
| Root  | `root`      | `<div>` | The band, carrying the background image when `photoUrl` is set |
| Title | `title`     | `<h2>`  | Switched to its on-photo treatment when there is a photo       |

{/* END: anatomy */}

{/* BEGIN: props */}

## API reference

<ArgTypes of={TopBannerStories} />

### `title`

Rendered as an `h2` inside the band. Omitted, the band is bare.

<Canvas of={TopBannerStories.Title} />

### `photoUrl`

Sets the background and switches the heading to its on-photo treatment.

<Canvas of={TopBannerStories.PhotoUrl} />

### `onBackClick`

Accepted for call-site parity with Mealdrop's own `TopBanner`, whose back button is commented out
in its source. Nothing here renders a control, so the callback never fires.

<Canvas of={TopBannerStories.OnBackClick} />

### `className`

Merged onto the root alongside the component's own `droppy-TopBanner` class.

<Canvas of={TopBannerStories.ClassName} />
{/* END: props */}
