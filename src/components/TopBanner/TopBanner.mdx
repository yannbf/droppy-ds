import { Anatomy } from '@component-anatomy/storybook/blocks'
import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks'
import * as TopBannerStories from './TopBanner.stories'

<Meta of={TopBannerStories} />

# TopBanner

{/* BEGIN: general */}

A full-width band at the top of a page, with a centered title over either a theme color or a
background photo — a category page's header, a restaurant's hero image.

```tsx
import { TopBanner } from '@droppy/design-system'
```

<Canvas of={TopBannerStories.Default} />

## Features

- Fixed 240px height, full width, centered content.
- Without `photoUrl`, the background is `--ds-color-surface-highlight`, the theme's accent
  surface color.
- With `photoUrl`, the band renders that image instead, covering the full band and centered —
  `photoUrl` takes any URL string, so it's the caller's job to host the image.
- `title` renders as a centered [Heading](?path=/docs/typography-heading--docs) at `level`
  (an `h1` by default); its color switches to `--ds-color-text-on-inverse` when a photo is set,
  since the theme's regular text color doesn't reliably read over an arbitrary photo.
- Merges any incoming `className` with its own `droppy-TopBanner` class.
- Accepts an `onBackClick` prop for call-site parity with the Mealdrop component this replaces,
  but renders no back control of its own — see [Related](#related) for composing one.

{/* END: general */}

{/* BEGIN: behavior */}

## How it works

With a photo behind it the heading takes its on-photo treatment, so the text holds contrast
against arbitrary imagery rather than trusting the surface token underneath.

<Canvas of={TopBannerStories.HeadingOverPhoto} />
{/* END: behavior */}

{/* BEGIN: anatomy */}

## Anatomy

<Canvas of={TopBannerStories.Anatomy} />

<Anatomy of={TopBannerStories.Anatomy} />

<span className="sb-sr-only">

| Part  | `data-part` | Renders                   | Purpose                                                        |
| :---- | :---------- | :------------------------ | :------------------------------------------------------------- |
| Root  | `root`      | `<div>`                   | The band, carrying the background image when `photoUrl` is set |
| Title | `title`     | `<h1>`–`<h5>` per `level` | Switched to its on-photo treatment when there is a photo       |

</span>

{/* END: anatomy */}
