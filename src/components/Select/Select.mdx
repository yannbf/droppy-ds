import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks'
import * as SelectStories from './Select.stories'

<Meta of={SelectStories} />

# Select

{/* BEGIN: anatomy */}

## Anatomy

A label above a wrapper that positions the chevron over the native control. There is no listbox part: the options are real `<option>` elements the platform renders.

<Canvas of={SelectStories.Anatomy} />

| Part    | `data-part` | Renders    | Purpose                                |
| :------ | :---------- | :--------- | :------------------------------------- |
| Root    | `root`      | `<div>`    | The field: label above, control below  |
| Label   | `label`     | `<label>`  | Tied to the control by `for`/`id`      |
| Wrapper | `wrapper`   | `<div>`    | Positions the chevron over the control |
| Control | `control`   | `<select>` | The native control                     |

{/* END: anatomy */}

{/* BEGIN: do-dont */}

## Do's and don'ts

### Do

- Write a `label` that directly names what's being chosen using the app vocabulary, like "Servings", rather than an action verb like "Select servings" or a generic naming like "Quantity"
- Order `options` the way the reader thinks about them (quantities ascending, delivery windows
  chronological, rest alphabetical) to complement native typeahead (which requires users to know options' names)
- Add an explicit "No choice yet" option when users can leave the field blank

### Don't

- Avoid mixing units inside one list ("2", "half a dozen") as the picker offers no way to group or
  explain them

{/* END: do-dont */}
