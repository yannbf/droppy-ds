import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks'
import * as SelectStories from './Select.stories'

<Meta of={SelectStories} />

# Select

{/* BEGIN: when-to-use */}

## When to use

- Choosing one value from a short, known list — quantities, delivery windows, and similar bounded option sets.
- Forms where the platform's native picker behavior on touch is preferable to a custom popup.

## When not to use

- To let someone type a value rather than pick from a fixed list, use [Input](?path=/docs/forms-input-input--docs) instead — a select can only ever return one of its declared options.
- For a list too long to scan by eye or by native typeahead alone, this component isn't it — Droppy has no searchable variant. Build a Search-first experience rather than stretching `Select`'s capabilities.

{/* END: when-to-use */}

{/* BEGIN: anatomy */}

## Anatomy

A label above a wrapper that positions the chevron over the native control. There is no listbox part: the options are real `<option>` elements the platform renders.

<Canvas of={SelectStories.Anatomy} />

| Part    | `data-part` | Renders    | Purpose                                |
| :------ | :---------- | :--------- | :------------------------------------- |
| Root    | `root`      | `<div>`    | The field: label above, control below  |
| Label   | `label`     | `<label>`  | Tied to the control by `for`/`id`      |
| Wrapper | `wrapper`   | `<div>`    | Positions the chevron over the control |
| Control | `control`   | `<select>` | The native control                     |

{/* END: anatomy */}

{/* BEGIN: when-to-use */}

## Related

- To collect free-form text instead of a fixed choice, use [Input](?path=/docs/forms-input-input--docs).

{/* END: when-to-use */}
