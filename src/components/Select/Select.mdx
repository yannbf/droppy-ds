import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks'
import * as SelectStories from './Select.stories'

<Meta of={SelectStories} />

# Select

{/* BEGIN: general */}

A single-choice control over a short, known list.

```tsx
import { Select } from '@droppy/design-system'
```

<Canvas of={SelectStories.Default} />

## Features

- **Renders a real native `<select>`**, so it gets the platform picker on touch, full native keyboard support, and OS-level accessibility for free — there's no scripted listbox to reimplement or get wrong.
- **Coerces numeric option values back to numbers** in `onChange`, so quantities and similar option sets arrive ready to use without a parsing step — while a genuinely non-numeric option is never coerced into `NaN`.
- **Generates a stable id via `useId`** when none is passed, so label/control association always works even if you never set one yourself.
- **Label sits before the control in the DOM**, matching reading order, while the focus highlight still reaches it via a `:has()` selector rather than a sibling combinator that would require the opposite order.
- **The chevron is a token-colored CSS mask**, not an SVG with a baked-in fill color, so it follows the theme into dark mode without any JavaScript theme lookup.

{/* END: general */}

{/* BEGIN: behavior */}

## How it works

### Value coercion

Numeric options come back as numbers and text options as strings, so a caller never parses the
value first — and a non-numeric option is never turned into `NaN`, which is what Mealdrop's
blanket `Number()` did (docs/MEALDROP-PARITY.md).

<Canvas of={SelectStories.ValueCoercion} />

### A native control

The control is the native `<select>`. On touch devices that opens the platform picker, which no
scripted listbox matches for accessibility or muscle memory — Droppy only restyles it and supplies
its own chevron.

<Canvas of={SelectStories.NativeControl} />
{/* END: behavior */}

{/* BEGIN: anatomy */}

## Anatomy

A label above a wrapper that positions the chevron over the native control. There is no listbox part: the options are real `<option>` elements the platform renders.

<Canvas of={SelectStories.Anatomy} />

| Part    | `data-part` | Renders    | Purpose                                |
| :------ | :---------- | :--------- | :------------------------------------- |
| Root    | `root`      | `<div>`    | The field: label above, control below  |
| Label   | `label`     | `<label>`  | Tied to the control by `for`/`id`      |
| Wrapper | `wrapper`   | `<div>`    | Positions the chevron over the control |
| Control | `control`   | `<select>` | The native control                     |

{/* END: anatomy */}
