import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks'
import * as ButtonStories from './Button.stories'

<Meta of={ButtonStories} />

# Button

{/* BEGIN: general */}

The primary action control — chrome comes from the theme, variants and the icon slot come from
Droppy.

```tsx
import { Button } from '@droppy/design-system'
```

<Canvas of={ButtonStories.Default} />

## Features

- Keyboard activation and disabled semantics come from Base UI's `Button` under the hood —
  Enter and Space work as expected, and `disabled` removes the control from the tab order and
  the accessibility tree.
- Chrome (fill, radius, focus ring, typeface, disabled state) comes from the theme layer; Droppy
  layers the `clear`, `round`, `large` variants and the icon slot on top.
- `icon` renders an `Icon` before the label, spaced with `gap` rather than a spacer element, and
  the icon takes the button's own text colour via `currentcolor` so it stays legible in both
  variants.
- Responds to `data-ds-theme` on the root element — fill and focus ring flip between light and dark
  automatically.
- Merges any incoming `className` instead of replacing its own, so wrapping it in `styled()` or
  adding utility classes composes rather than strips the design system's styling.
- Padding scales up at the `768px` breakpoint so the control doesn't feel undersized on larger
  viewports.

{/* END: general */}

{/* BEGIN: when-to-use */}

## When to use

- Primary and secondary in-page actions — a page's one obvious primary action, plus its
  secondary ones as `clear`.
- Icon-augmented actions, where the icon reinforces the label ("Order now" with a cart icon).
- A round, icon-only dismissal on a themed surface (`round`, `clear`, `icon`, no children) — for
  example a modal's close control.

## When not to use

- For navigation, use a real `<a>` styled with CSS rather than a `Button` with an `onClick`
  that changes the URL — `Button` always renders a `<button>`, so a navigating one loses
  native link behaviour (right-click, open-in-new-tab, visible link previews).
- For a floating icon-only affordance that has to stay legible over arbitrary imagery (carousel
  arrows, overlay dismissals), use [IconButton](?path=/docs/actions-iconbutton--docs) instead,
  because it is deliberately not theme-reactive and carries its own drop shadow for contrast.

{/* END: when-to-use */}

{/* BEGIN: behavior */}

## How it works

### Icon-only buttons need a label

Drop `children` and the button has no text to read, so its accessible name has to come from
`aria-label`. The glyph itself is `aria-hidden` and contributes nothing.

<Canvas of={ButtonStories.IconOnlyNeedsALabel} />

### The variant vocabulary

The variants side by side — one obvious primary per screen, `clear` for the secondary beside it.

<Canvas of={ButtonStories.Variants} />
{/* END: behavior */}

{/* BEGIN: anatomy */}

## Anatomy

The control and its optional leading glyph. There is no label part: `children` render directly inside the button, spaced from the icon with `gap` rather than a spacer node.

<Canvas of={ButtonStories.Anatomy} />

| Part | `data-part` | Renders    | Purpose                                                      |
| :--- | :---------- | :--------- | :----------------------------------------------------------- |
| Root | `root`      | `<button>` | Fill, radius, focus ring, and the disabled state             |
| Icon | `icon`      | `<svg>`    | The optional leading glyph, rendered only when `icon` is set |

{/* END: anatomy */}

{/* BEGIN: examples */}

## Examples

### Mealdrop cart panel

<Canvas of={ButtonStories.MealdropCheckoutActions} />

{/* END: examples */}

{/* BEGIN: do-dont */}

## Do's and don'ts

### Do

- Use `Button` for things that happen on the page: submitting a form, opening a panel, adding to
  a cart.
- Give icon-only buttons (`icon` with no `children`) an `aria-label` — the icon alone gives the
  button no name.

### Don't

- Don't use `Button` to go to another page — use [Link](?path=/docs/actions-link--docs). A real
  link is announced as one and supports right-click and opening in a new tab.
- Don't put a `Button` inside a `Link` (or any control inside another control) — two controls in
  one spot confuse keyboard focus and screen readers. If it navigates, it's a link.
- Avoid `Button` for a floating icon-only control on top of a photo — that's
  [IconButton](?path=/docs/actions-iconbutton--docs)'s job.

{/* END: do-dont */}

{/* BEGIN: a11y */}

## Keyboard & screen reader

| Key   | Action                                           |
| ----- | ------------------------------------------------ |
| Enter | Activates the button (native behaviour)          |
| Space | Activates the button on keyup (native behaviour) |
| Tab   | Moves focus to/from the button, unless disabled  |

- **ARIA**: renders a real `<button>`, so there's no explicit role to manage — native button
  semantics apply as-is.
- **Screen reader**: `children` is announced as the accessible name.
- **You must provide:** `aria-label` on icon-only buttons (`icon` with no `children`) — the icon
  is `aria-hidden`, so without a label the button has no accessible name.
- **Known limitation:** disabling a button always removes it from the tab order and the
  accessibility tree — there's no `focusableWhenDisabled`-style escape hatch for a loading state
  that should stay focusable.

{/* END: a11y */}

{/* BEGIN: brand */}

## Brand

The [brand guidelines](?path=/docs/brand-guidelines--docs) shape buttons in three ways:

- "The grey ramp carries every surface and almost all text, including the primary button." A
  primary button is grey, not colorful.
- Teal is Droppy talking about itself, "not for buttons, links, or interface chrome" — no teal
  buttons.
- Buttons use Hind, the "text and UI face for body copy, labels, and buttons", in sentence case —
  never the display face, never ALL CAPS. A call to action earns attention through its fill, not
  its lettering.

{/* END: brand */}

{/* BEGIN: styling */}

## Styling

- `--ds-space-xs` — gap between the icon and the label.
- `--ds-radius-pill` — border radius when `round` is set.
- `--ds-color-text-primary` — text colour for the `clear` variant.
- `--ds-color-action-subtle-hover` — hover fill for the `clear` variant.
- `className` merges with the component's own classes rather than replacing them.

Everything else — the base fill, focus ring, and typeface — comes from the theme layer, which
this component doesn't override.

{/* END: styling */}

{/* BEGIN: props */}

## API reference

<ArgTypes of={ButtonStories} />

### `children`

The label. Omitted, the button is icon-only and needs an `aria-label`.

<Canvas of={ButtonStories.Children} />

### `clear`

Strips the fill, for the secondary action beside a primary one.

<Canvas of={ButtonStories.Clear} />

### `large`

Adds height, for the one action a screen is really about.

<Canvas of={ButtonStories.Large} />

### `round`

Fully rounds the control — pair it with an icon and no label.

<Canvas of={ButtonStories.Round} />

### `icon`

Renders a glyph before the label, spaced with `gap` rather than a spacer element — one fewer DOM
node than Mealdrop's original, for the same 1rem.

<Canvas of={ButtonStories.IconProp} />

### `iconSize`

Overrides the glyph size without touching the button's own padding.

<Canvas of={ButtonStories.IconSize} />

### `disabled`

Dims the control and stops it firing `onClick`.

<Canvas of={ButtonStories.Disabled} />

### `className`

Merged onto the root alongside the component's own `droppy-Button` class.

<Canvas of={ButtonStories.ClassName} />
{/* END: props */}

{/* BEGIN: history */}

## Decision log

- **2025-03**: the `icon` prop was retyped from plain `string` to the `IconName` string-literal union, making invalid icon names a compile-time error; Droppy inherits this closed, typed icon set ([IconName union](https://github.com/yannbf/mealdrop/commit/14cf17a324a99c4744eec1495810f96d4e17bc5d), [mealdrop #42](https://github.com/yannbf/mealdrop/pull/42)).
- **2026-07**: Button swapped its internals to Base UI's Button while preserving Mealdrop's boolean-modifier API (`clear`, `round`, `large`, `icon`, `iconSize`) rather than a variant enum, so every call site — including icon-only close buttons like `<Button clear round icon="cross" />` — swapped without prop changes; styles moved onto tokens with a single :focus-visible ring and data-disabled styling ([buttons migration commit](https://github.com/yannbf/mealdrop/commit/c3c8adeb8e81c6e2c545b65728ed43a1aaf5d21e), [migration commit](https://github.com/yannbf/mealdrop/commit/dea29009cb06ec78eb7e2c52f216da78d913e747)).
- **2026-07**: buttons follow the [brand guidelines](?path=/docs/brand-guidelines--docs) type roles — Hind is the "text and UI face for body copy, labels, and buttons", set at the small step and regular weight, never the display face or ALL CAPS — so a CTA earns attention through its fill rather than its typography; dark mode re-decides the primary action's role rather than inverting it (neutral in light, soft green becoming "the primary button" in dark).
- **2026-07**: icon-to-label spacing moved to CSS `gap` instead of Mealdrop's dedicated spacer element — one fewer DOM node for the same 1rem — with the icon slot as a typed `icon?: IconName` prop rendering before the label ([Mealdrop parity doc](https://github.com/yannbf/droppy-ds/blob/main/docs/MEALDROP-PARITY.md), [initial component set](https://github.com/yannbf/droppy-ds/commit/8790c8460335870f6ca26be9850710ff6023cc61)).
- **2026-07**: the theme ships `line-height: 1` and `gap: 0.5rem` as brand defaults; Mealdrop had to restore `line-height: normal` and zero gap in overrides to keep its original 54px height, documenting Button's height and built-in icon gap as theme opinions that diverge from the pre-migration design ([button height commit](https://github.com/yannbf/mealdrop/commit/8de6ca1aecc1febd479ed80245430c1d810dcf66)).
- **Built on Base UI [`Button`](https://base-ui.com/react/components/button).** Droppy supplies the props, the theme classes, and the copy around it.

{/* END: history */}

{/* BEGIN: known-issues */}

## Known issues & open questions

- [#45](https://github.com/yannbf/droppy-ds/issues/45): onClick is typed () =&gt; void, so standard MouseEventHandler callbacks fail to compile and the runtime event is hidden. **Workaround:** cast the handler to ButtonProps['onClick'] (event is delivered at runtime) or use onClickCapture/onPointerDown which keep native typing.
- [#47](https://github.com/yannbf/droppy-ds/issues/47): `large` is silently negated whenever `icon` is set — `--withIcon` padding overrides `--large` at equal specificity. **Workaround:** add a custom className and restore the large padding with a two-class selector that out-specifies .droppy-Button--withIcon.
- [#50](https://github.com/yannbf/droppy-ds/issues/50): unrecognized `icon` string crashes the render tree — Icon's lookup is unguarded. **Workaround:** validate dynamic names against the exported iconNames array before passing, and keep an error boundary around CMS-driven UI.
- [#52](https://github.com/yannbf/droppy-ds/issues/52): setting `disabled` while focused drops keyboard focus to &lt;body&gt;, and the component exposes no counter-measure — the library's own QuantityStepper triggers it. **Workaround:** keep the button enabled with a guarded handler + aria-disabled, or spread the untyped focusableWhenDisabled through rest-props.
- [#54](https://github.com/yannbf/droppy-ds/issues/54): disabled-state docs make two false claims: removal from the accessibility tree, and "no built-in way" to stay focusable. **Workaround:** unmount or aria-hidden-wrap buttons that must be hidden from AT; spread the untyped focusableWhenDisabled for focusable disabled states.
- [#56](https://github.com/yannbf/droppy-ds/issues/56): `icon="star"` renders as a hairline outline instead of the filled glyph — only `stroke` is bridged to currentcolor and star is the set's one fill-based path. **Workaround:** scope `fill: currentcolor; stroke: none` to the specific button's .droppy-Icon in app CSS (never globally).
- [#122](https://github.com/yannbf/droppy-ds/issues/122): components are plain function components, so `ref` type-checks but never attaches on React 18 (works on React 19 only).
- [#129](https://github.com/yannbf/droppy-ds/issues/129): focus indicators are box-shadow-only with `outline: none`, so they vanish entirely in Windows High Contrast mode.
- [#133](https://github.com/yannbf/droppy-ds/issues/133): the shared `--ds-shadow-focus` ring is below the 3:1 non-text contrast minimum in light theme.
- [#138](https://github.com/yannbf/droppy-ds/issues/138): theme chrome is bound to unnamespaced global classes (`.Button`, `.Input`, `.FieldRoot`, …) with no cascade layer, so host-app CSS collides in both directions.

{/* END: known-issues */}

{/* BEGIN: when-to-use */}

## Related

- Icon-only, theme-independent affordance over imagery — see
  [IconButton](?path=/docs/actions-iconbutton--docs).
- The icon set `Button` draws its `icon` prop from — see [Icon](?path=/docs/media-content-icon--docs).

{/* END: when-to-use */}
