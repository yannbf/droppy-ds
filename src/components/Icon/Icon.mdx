import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks'
import * as IconStories from './Icon.stories'

<Meta of={IconStories} />

# Icon

{/* BEGIN: when-to-use */}

## When to use

- Inside a control that already carries (or will carry) an accessible name — `Button`'s `icon`
  prop, or `IconButton`'s `name` prop.
- As a standalone decorative glyph where the surrounding text or layout conveys the meaning.

## When not to use

- As the sole content of a clickable element with no accessible name of its own — `Icon` is
  `aria-hidden` by design, so pair it with a labelled control such as
  [Button](?path=/docs/actions-button--docs) or
  [IconButton](?path=/docs/actions-iconbutton--docs) rather than attaching a click handler
  directly to a bare `Icon`.

{/* END: when-to-use */}

{/* BEGIN: anatomy */}

## Anatomy

`Icon` is a single-part component: one `<svg>`, whose paths come from the bundled set that `name` indexes into. Nothing inside it is addressable.

<Canvas of={IconStories.Anatomy} />

| Part | `data-part` | Renders | Purpose                                                               |
| :--- | :---------- | :------ | :-------------------------------------------------------------------- |
| Root | `root`      | `<svg>` | The glyph: viewBox, stroke colour, and the width/height/minWidth trio |

{/* END: anatomy */}

{/* BEGIN: when-to-use */}

## Related

- The icon-before-label slot on actions — see [Button](?path=/docs/actions-button--docs).
- The icon-only floating control that renders an `Icon` internally — see
  [IconButton](?path=/docs/actions-iconbutton--docs).

{/* END: when-to-use */}
