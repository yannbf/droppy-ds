import { Meta } from '@storybook/addon-docs/blocks'
import { ComponentBrowser } from './ComponentBrowser'

<Meta title="Component browser (Icons)" />

# Component browser

Every Droppy component, grouped by category. Each card shows a schematic archetype preview.
Type to filter by name or description, and click a card to open that component's docs page,
which leads with its primary story.

<ComponentBrowser preview="baseui" />
