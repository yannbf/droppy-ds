import { Meta } from '@storybook/addon-docs/blocks'
import {
  Diagnostics,
  ColorPalette,
  ColorTable,
  FontFamilyPreview,
  FontWeightScale,
  DimensionScale,
  TokenTable,
  ShadowPreview,
  MotionPreview,
  TokenDetail,
  TypographyScale,
} from '@unpunnyfuns/swatchbook-addon'

<Meta title="Design tokens/Color" tags={['general-tokens']} />

# Color tokens

**Palette primitives** (`color.palette.*`) are the raw color values used in Droppy.
**Semantic roles** (everything else under `color.*`) serve specific purposes,
like a surface, a piece of text, a border. Everyday component work should only use
semantic colors. Semantic colors ensure consistent branding, and provide dark theme
support.

## Semantic roles

Each role resolves to one of the colors in the primitive palettes below, depending on whether the `light` or `dark` mode is used.

<ColorTable filter="color.*.*" caption="" />

## Primitive color palettes

<ColorPalette filter="color.palette.neutral.**" caption="" />

<ColorPalette filter="color.palette.green.**" caption="" />

<ColorPalette filter="color.palette.blue.**" caption="" />

<ColorPalette filter="color.palette.red.**" caption="" />

<ColorPalette filter="color.palette.brand.**" caption="" />
