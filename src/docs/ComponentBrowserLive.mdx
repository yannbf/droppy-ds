import { Meta } from '@storybook/addon-docs/blocks'

import { ComponentBrowser } from './ComponentBrowser'

<Meta title="Component browser (Live)" />

# Component browser

Every Droppy component, grouped by category. Each card embeds the component's own primary
story — rendered live in an isolated preview frame, frozen on its finished state. Type to
filter by name or description, and click a card to open that component's docs page.

<ComponentBrowser preview="live" />
